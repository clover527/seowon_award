# 🔧 Render.com 빌드 문제 해결

## 문제
"앱을 먼저 빌드해주세요" 메시지가 표시됩니다.

## 해결 방법

### Render.com 설정 확인

Render.com 대시보드에서 다음 설정을 확인하세요:

#### 1. Build Command 확인
**Settings** → **Build & Deploy** → **Build Command**

다음 중 하나로 설정:
```
npm install && npm run build
```

또는:
```
npm install && vite build
```

#### 2. Start Command 확인
**Settings** → **Build & Deploy** → **Start Command**

다음으로 설정:
```
npm start
```

#### 3. Root Directory 확인
**Settings** → **Build & Deploy** → **Root Directory**

비워두거나 `./` 로 설정

---

## 중요: 빌드가 실행되었는지 확인

Render.com의 **Logs** 탭에서 다음을 확인:

1. ✅ `npm install`이 실행되었는가?
2. ✅ `vite build`가 실행되었는가?
3. ✅ `dist` 폴더가 생성되었는가?

빌드 로그에서 다음과 같은 메시지가 보여야 합니다:
```
> vite build
vite v5.4.21 building for production...
✓ 22 modules transformed.
✓ built in XXXms
```

---

## 수동 재배포

1. Render.com 대시보드에서 서비스 클릭
2. **"Manual Deploy"** 버튼 클릭
3. **"Deploy latest commit"** 선택
4. 배포가 완료될 때까지 대기 (5-10분)

---

## 확인 사항 체크리스트

- [ ] Build Command가 `npm install && npm run build`로 설정됨
- [ ] Start Command가 `npm start`로 설정됨
- [ ] 환경 변수 `NODE_ENV=production` 설정됨
- [ ] 빌드 로그에서 `vite build` 성공 확인
- [ ] 서버 로그에서 "정적 파일 서빙 활성화" 메시지 확인

---

## 여전히 안 되면

서버 로그를 확인하고 알려주세요:
1. Render.com → 서비스 → **Logs** 탭
2. 오류 메시지 확인
3. 로그 내용 공유

해결 도와드리겠습니다!
